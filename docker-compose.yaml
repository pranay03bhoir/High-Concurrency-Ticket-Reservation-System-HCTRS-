services:
  redis:
    image: redis:7.2-alpine
    container_name: redis_dev
    restart: unless-stopped
    ports:
      - "6379:6379"
    command: redis-server --requirepass ${REDIS_PASSWORD:-pranay123} --appendonly yes
    volumes:
      - redis_data:/data

volumes:
  redis_data: